#include <iostream>
#include "Graph.h"

int main() {
    Index index;
    index.insertNode(3);
    index.insertNode(4);
    index.insertNode(5);
    index.insertNode(6);
    index.insertNode(7);
    index.insertNode(8);
    index.print();

    return 0;
}